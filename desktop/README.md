# fastgit desktop (Wails v3)

## Run in dev mode

```bash
cd desktop/frontend
npm install
npm run build
cd ..
go run .
```

> 如果你本机安装了 `wails3`，也可使用 `wails3 dev`。

## Build desktop app

```bash
wails3 build
```
